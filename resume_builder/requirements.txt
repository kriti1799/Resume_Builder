requests>=2.28.0
beautifulsoup4>=4.12.0
openai>=1.0.0
anthropic>=0.40.0
reportlab>=4.0.0
pdfplumber>=0.10.0
pymupdf>=1.24.0
# PDF → Word → edit → PDF (preserves layout; edit in Word)
pdf2docx>=0.5.0
python-docx>=1.0.0
docx2pdf>=0.1.8
# Optional: Docling path — parse PDF to Markdown, LLM tailor, pandoc → PDF (install pandoc separately)
docling>=2.0.0
# Optional: better PDF→LaTeX conversion (commercial; pip install aspose-pdf)
aspose-pdf
